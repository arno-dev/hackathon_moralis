import 'package:shared_preferences/shared_preferences.dart';

class IPFSService {
  final SharedPreferences sharedPreferences;
  IPFSService(this.sharedPreferences);

  /// Retrieve the encryption key from wallet
  /// @param {WalletCredential} wallet - the sender
  /// @param {WalletCredential} dest - the receiver
  
}
